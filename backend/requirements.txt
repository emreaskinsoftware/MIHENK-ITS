# MİHENK — Python bağımlılıkları
#
# Sürümler, projenin GERÇEKTEN ÜZERİNDE KOŞTUĞU ve testlerin geçtiği sürümlerdir
# (21 Ağustos 2026). Denenmemiş sürüm numarası yazmıyoruz: "çalışması beklenen"
# bir bağımlılık listesi, demo sabahı çalışmayan bir kurulum demektir.
#
# Alt sınır (>=) yerine sabit (==) tercih edildi; yarışma teslimi tekrar
# üretilebilir olmalı.

# --- Çekirdek servis ---
fastapi==0.135.2
uvicorn[standard]==0.42.0
pydantic==2.12.5
pydantic-settings==2.13.1

# --- Sayısal / kümeleme / model kalıcılığı ---
numpy==1.26.4
scikit-learn==1.5.0
joblib==1.5.3

# --- Ölçüm ---
PyYAML==6.0.3

# --- LLM sağlayıcı (bkz. app/llm/provider.py) ---
# Yalnızca MIHENK_LLM_PROVIDER=api kullanılacaksa gerekir; varsayılan
# FakeProvider hiçbir dış bağımlılık istemez.
anthropic==0.42.0

# --- Gömme ve tespit modeli (ml/ tarafı) ---
# ÖNEMLİ: torch bu dosyada YOK; sürümü donanıma göre ayrı kurulur.
#   NVIDIA GPU : pip install torch --index-url https://download.pytorch.org/whl/cu124
#   CPU        : pip install torch --index-url https://download.pytorch.org/whl/cpu
# Sistem cihazı kendisi seçer (MIHENK_DETECTION_DEVICE=auto); CUDA bulunursa
# hem gömme hem de ince ayar GPU'ya taşınır (bkz. app/detection/device.py).
sentence-transformers==6.0.0
transformers==5.15.1

# --- Test ---
pytest==9.0.2
httpx==0.28.1
